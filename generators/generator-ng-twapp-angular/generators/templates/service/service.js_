'use strict';
/**
 * @ngdoc service
 * @name <%= moduleName %>.<%= name %>
 * @description A service for the <%= moduleName %> module.
 */
angular.module('<%= moduleName %>').service('<%= name %>', function() {
	var service = {};
	
	return service;
});