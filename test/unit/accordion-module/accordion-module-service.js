'use strict';

/*describe('Service: accordionService', function () {
    var mainService;
    // load the controller's module
    beforeEach(module('ng-twapp.accordion'));

    // Initialize the service
    beforeEach(inject(function (_accordionService_) {
    	accService = _accordionService_
    }));

    it('should contain an $ service accordionService',
        inject(function () {
            expect(accordionService).not.to.equal(null);
        }));

    it('should return correct data object from getAccordionData() method', function () {
        var data = accordionService.getAccordionData();
        expect(data.desc).not.to.equal(null);
    });
});*/
